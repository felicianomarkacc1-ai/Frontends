import React from 'react';
import ProgressTracker from '../ProgressTracker';

const ProgressTrackerContent: React.FC = () => {
  return (
    <div className="embedded-content">
      <ProgressTracker />
    </div>
  );
};

export default ProgressTrackerContent;